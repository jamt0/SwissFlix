name: swissflix
description: "Swiss Medical Challenge"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.7.0
  dartz: ^0.10.1
  bloc: ^8.1.4
  equatable: ^2.0.5
  go_router: ^14.4.1
  flutter_bloc: ^8.1.6
  google_fonts: ^6.2.1
  infinite_scroll_pagination: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true